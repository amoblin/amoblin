# This file is generated manually by NetXRay@byhh.
# It isn't very elagent. Don't mock me :)

CC=pdflatex

all:
	pdflatex amoblin.tex

%.pdf:%.tex
	$(CC) $^
clean:
	rm *.aux *.pdf *.log
rebuild:
	make clean all
